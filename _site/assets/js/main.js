document.addEventListener('DOMContentLoaded', () => {
    document.querySelector('.modal-trigger').addEventListener('click', event => {
      // render the modal
      console.log("trigger detected")
    })
})